// Main content region. <main> marks the document's primary content so
// assistive tech can skip directly to it. Sections are filled in later
// exercises (hero, highlights, testimonials, about, booking).
function Main() {
  return (
    <main className="main">
      Homepage
    </main>
  );
}

export default Main;
